/**
 * Key Management Service Interfaces barrel export
 * Provides centralized access to all key management service interface definitions
 */

export type {
  IKeyManagementService,
  PublicKeyJWK,
  DecryptedKeyPair,
} from './IKeyManagementService.js';
