<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Authorize <%= client.name %></title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }

    .consent-container {
      background: white;
      border-radius: 12px;
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
      max-width: 480px;
      width: 100%;
      overflow: hidden;
    }

    .consent-header {
      background: #f8f9fa;
      padding: 30px;
      text-align: center;
      border-bottom: 1px solid #e9ecef;
    }

    .client-logo {
      width: 80px;
      height: 80px;
      border-radius: 12px;
      margin: 0 auto 20px;
      display: block;
      object-fit: cover;
    }

    .client-logo-placeholder {
      width: 80px;
      height: 80px;
      border-radius: 12px;
      margin: 0 auto 20px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 32px;
      font-weight: bold;
      color: white;
    }

    .consent-header h1 {
      font-size: 24px;
      color: #212529;
      margin-bottom: 10px;
    }

    .consent-header p {
      color: #6c757d;
      font-size: 14px;
    }

    .consent-body {
      padding: 30px;
    }

    .permission-label {
      font-size: 16px;
      font-weight: 600;
      color: #212529;
      margin-bottom: 15px;
      display: block;
    }

    .permissions-list {
      background: #f8f9fa;
      border-radius: 8px;
      padding: 20px;
      margin-bottom: 25px;
    }

    .permission-item {
      display: flex;
      align-items: start;
      margin-bottom: 12px;
      font-size: 14px;
      color: #495057;
    }

    .permission-item:last-child {
      margin-bottom: 0;
    }

    .permission-icon {
      color: #667eea;
      margin-right: 10px;
      font-size: 16px;
      flex-shrink: 0;
      margin-top: 2px;
    }

    .client-info {
      font-size: 13px;
      color: #6c757d;
      margin-bottom: 25px;
      padding: 15px;
      background: #f8f9fa;
      border-radius: 8px;
      line-height: 1.6;
    }

    .client-info a {
      color: #667eea;
      text-decoration: none;
    }

    .client-info a:hover {
      text-decoration: underline;
    }

    .button-group {
      display: flex;
      gap: 12px;
    }

    .btn {
      flex: 1;
      padding: 14px 24px;
      border: none;
      border-radius: 8px;
      font-size: 16px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s ease;
      text-align: center;
    }

    .btn-primary {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
    }

    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
    }

    .btn-secondary {
      background: white;
      color: #6c757d;
      border: 2px solid #dee2e6;
    }

    .btn-secondary:hover {
      background: #f8f9fa;
      border-color: #ced4da;
    }

    .security-notice {
      margin-top: 20px;
      padding: 12px;
      background: #e7f3ff;
      border-left: 4px solid #0056b3;
      border-radius: 4px;
      font-size: 12px;
      color: #004085;
    }
  </style>
</head>
<body>
  <div class="consent-container">
    <div class="consent-header">
      <% if (client.logo_url) { %>
        <img src="<%= client.logo_url %>" alt="<%= client.name %> logo" class="client-logo">
      <% } else { %>
        <div class="client-logo-placeholder">
          <%= client.name.charAt(0).toUpperCase() %>
        </div>
      <% } %>
      <h1><%= client.name %></h1>
      <p>wants to access your account</p>
    </div>

    <div class="consent-body">
      <form method="POST" action="/oauth/consent">
        <input type="hidden" name="client_id" value="<%= requestParams.client_id %>">
        <input type="hidden" name="redirect_uri" value="<%= requestParams.redirect_uri %>">
        <input type="hidden" name="scope" value="<%= requestParams.scope %>">
        <input type="hidden" name="code_challenge" value="<%= requestParams.code_challenge %>">
        <input type="hidden" name="code_challenge_method" value="<%= requestParams.code_challenge_method %>">
        <% if (requestParams.state) { %>
          <input type="hidden" name="state" value="<%= requestParams.state %>">
        <% } %>

        <label class="permission-label">This application will be able to:</label>

        <div class="permissions-list">
          <%
            const scopes = scope.split(' ');
            const scopeDescriptions = {
              'openid': 'Access your basic profile information',
              'profile': 'View your full profile details',
              'email': 'Access your email address',
              'offline_access': 'Maintain access when you\'re not using the app',
            };

            scopes.forEach(function(scopeItem) {
              const description = scopeDescriptions[scopeItem] || scopeItem;
          %>
            <div class="permission-item">
              <span class="permission-icon">✓</span>
              <span><%= description %></span>
            </div>
          <% }); %>
        </div>

        <% if (client.privacy_url || client.terms_url || client.homepage_url) { %>
          <div class="client-info">
            Learn more about <%= client.name %>:
            <% if (client.homepage_url) { %>
              <a href="<%= client.homepage_url %>" target="_blank">Website</a>
            <% } %>
            <% if (client.privacy_url) { %>
              • <a href="<%= client.privacy_url %>" target="_blank">Privacy Policy</a>
            <% } %>
            <% if (client.terms_url) { %>
              • <a href="<%= client.terms_url %>" target="_blank">Terms of Service</a>
            <% } %>
          </div>
        <% } %>

        <div class="button-group">
          <button type="submit" name="allow" value="false" class="btn btn-secondary">
            Cancel
          </button>
          <button type="submit" name="allow" value="true" class="btn btn-primary">
            Authorize
          </button>
        </div>

        <div class="security-notice">
          <strong>Security tip:</strong> Only authorize applications you trust. You can revoke access at any time from your account settings.
        </div>
      </form>
    </div>
  </div>
</body>
</html>
